import 'package:notes_frontend/main.dart' as app;

/// PUBLIC_INTERFACE
/// CLI entry point to launch the Flutter app if invoked via `dart run`.
/// Some CI tools look for a `bin/` entry to determine project root.
void main(List<String> args) {
  app.main();
}
