import 'package:notes_frontend/notes_frontend.dart';

/// Minimal entrypoint for analyzer/CI discovery. Not used at runtime.
void main() {
  // Touch exports to ensure they are included in the graph.
  // ignore: avoid_print
  print(DateTime.now());
}
