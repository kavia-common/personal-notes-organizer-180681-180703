# This file tracks properties of this Flutter project.
# It can help tools and CI runners recognize the Flutter project root.

version:
  revision: unknown
  channel: stable

project_type: app

# Files and directories created by Flutter tools will be stored in this location.
# This ensures commands like `flutter analyze` and `flutter test` can detect the root.
