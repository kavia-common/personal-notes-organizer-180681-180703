This directory is normally generated by `flutter pub get`.
If CI cannot run `flutter pub get` before analysis, it may not detect the project root.

Please ensure CI runs:
  cd personal-notes-organizer-180681-180703/notes_frontend
  flutter pub get
Then run `flutter analyze` and `flutter test`.
